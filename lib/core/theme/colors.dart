import 'package:flutter/material.dart';

/// Global brand accent (Kotatsu-style blue). Used across nav, badges,
/// reader state markers, switches and gradients.
const Color kAccentColor = Color(0xFF4C8DFF);

/// Light tint of the accent for icons/text on dark surfaces.
const Color kAccentLight = Color(0xFFA5C8FF);

/// Darker accent for gradients / deep fills.
const Color kAccentDark = Color(0xFF2B5CE6);

/// Tinted surface for accent chip/card backgrounds.
const Color kAccentSurface = Color(0xFF22304D);